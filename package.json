{
  "name": "freesites-generator",
  "version": "0.1.0",
  "description": "FreeSites AI contractor website generator",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev --no-open",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "@netlify/blobs": "^8.0.0",
    "@netlify/functions": "^2.0.0",
    "adm-zip": "^0.5.10",
    "resend": "^3.2.0",
    "stripe": "^14.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
